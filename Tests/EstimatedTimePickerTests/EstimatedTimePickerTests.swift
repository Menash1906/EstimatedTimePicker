import XCTest
@testable import EstimatedTimePicker

final class EstimatedTimePickerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(EstimatedTimePicker().text, "Hello, World!")
    }
}
